"use client";
import {useEffect,useState} from "react";
import {useSearchParams,useRouter} from "next/navigation";
export default function WinnerClaimClient(){const params=useSearchParams(),router=useRouter();const[email,setEmail]=useState(params.get("email")||""),[customers,setCustomers]=useState([]),[loading,setLoading]=useState(false),[selected,setSelected]=useState(null),[msg,setMsg]=useState("");useEffect(()=>{const e=params.get("email");if(e)setEmail(e)},[params]);async function find(){const e=email.trim().toLowerCase();if(!e||!e.includes("@"))return setMsg("Please enter your email address.");setLoading(true);setMsg("");setSelected(null);try{const r=await fetch(`/api/winner-claim?email=${encodeURIComponent(e)}`,{cache:"no-store"}),j=await r.json();if(!r.ok)throw Error(j.error||"Could not find your winner profile.");setCustomers(j.customers||[]);if(!(j.customers||[]).length)setMsg("We couldn't find an active winner gift for this email address. Please make sure you use the same email address that VALE BEAUTY has on file.")}catch(e){setMsg(e.message)}finally{setLoading(false)}}function choose(c){setSelected(c);localStorage.setItem("vale-customer-session",JSON.stringify({id:c.id,name:c.name,email:c.email}));router.push("/services")}return <div className="bookingbox" style={{maxWidth:680,margin:"40px auto"}}><div className="eyebrow">VALE BEAUTY</div><h1>Your Winner Gift 🎁</h1><p className="muted">Enter the email address we used when you registered your winner profile.</p><div className="form"><label>Email<input type="email" value={email} onChange={e=>{setEmail(e.target.value);setCustomers([]);setMsg("")}} placeholder="you@example.com" required/></label><button className="btn" type="button" onClick={find} disabled={loading}>{loading?"Checking…":"Continue"}</button></div>{customers.length>0&&<div style={{marginTop:24}}><div className="fieldLabel">Choose your winner profile</div><p className="muted small">If you have more than one profile with this email, choose the name that won.</p><div className="profilePicker">{customers.map(c=><button type="button" className={`profileChoice ${selected?.id===c.id?"selected":""}`} key={c.id} onClick={()=>choose(c)}><strong>{c.name}</strong><span>{c.email}</span><small style={{display:"block",marginTop:5}}>🎁 {c.rewards.join(" · ")}</small></button>)}</div></div>}{msg&&<div className="error" style={{marginTop:16}}>{msg}</div>}</div>}
