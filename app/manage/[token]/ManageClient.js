"use client";import {useState} from "react";export default function ManageClient({token}){const [email,setEmail]=useState("");const [msg,setMsg]=useState("");async function go(action){const r=await fetch(`/api/manage/${token}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,action})});const j=await r.json();setMsg(r.ok?j.message:(j.error||"We could not verify this booking."))}return <div className="bookingbox"><form className="form" onSubmit={e=>{e.preventDefault();go("verify")}}><label>Email used for the booking<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="btn">Verify</button></form>{msg&&<div className="success" style={{marginTop:15}}>{msg}</div>}<div style={{display:"flex",gap:10,marginTop:15}}><button className="btn" onClick={()=>go("cancel")}>Cancel Appointment</button><button className="btn" onClick={()=>setMsg("To reschedule, contact the studio after verification.")}>Reschedule</button></div></div>}
