import {NextResponse} from "next/server";
import crypto from "crypto";
import {DateTime} from "luxon";
import {createClient} from "@supabase/supabase-js";
import {sendMail} from "../../../lib/mailer";
import {business,findService,findAddon} from "../../../lib/services";
const OPEN_MINUTES=480,CLOSE_MINUTES=1140,SLOT_MINUTES=30,BUFFER_MINUTES=30;
const db=()=>createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}});
async function exceptionBlocks(client,date){const dayStart=DateTime.fromISO(`${date}T00:00:00`,{zone:business.timezone}).toUTC().toISO(),dayEnd=DateTime.fromISO(`${date}T23:59:59`,{zone:business.timezone}).toUTC().toISO();const {data,error}=await client.from("availability_exceptions").select("start_at,end_at,kind,reason").lt("start_at",dayEnd).gt("end_at",dayStart);if(error&&error.code!=="42P01")throw error;return(data||[]).map(x=>{const a=DateTime.fromISO(x.start_at).setZone(business.timezone),z=DateTime.fromISO(x.end_at).setZone(business.timezone);return{start_minutes:a.hour*60+a.minute,end_minutes:z.hour*60+z.minute}})}
function mins(t){const [h,m]=String(t).split(":").map(Number);return h*60+m}
function slotAvailability(blocked,duration,date){const now=DateTime.now().setZone(business.timezone),out=[];for(let start=OPEN_MINUTES;start+duration+BUFFER_MINUTES<=CLOSE_MINUTES;start+=SLOT_MINUTES){const startAt=DateTime.fromISO(`${date}T${String(Math.floor(start/60)).padStart(2,"0")}:${String(start%60).padStart(2,"0")}`,{zone:business.timezone});if(startAt<=now)continue;const endWithBuffer=start+duration+BUFFER_MINUTES;if(!blocked.some(b=>start<b.end_minutes&&endWithBuffer>b.start_minutes))out.push(`${String(Math.floor(start/60)).padStart(2,"0")}:${String(start%60).padStart(2,"0")}`)}return out}
export async function GET(req){try{const {searchParams}=new URL(req.url),date=searchParams.get("date"),month=searchParams.get("month"),duration=Number(searchParams.get("duration"));if((!date&&!month)||!Number.isFinite(duration)||duration<=0)return NextResponse.json({error:"Date and appointment duration are required."},{status:400});const client=db();let query=client.from("bookings").select("date,start_minutes,duration_minutes,status").eq("status","confirmed");if(date)query=query.eq("date",date);else query=query.gte("date",`${month}-01`).lt("date",DateTime.fromISO(`${month}-01`).plus({months:1}).toISODate()||"");const {data,error}=await query;if(error)throw error;const grouped=new Map();for(const b of data||[]){const start=Number(b.start_minutes),end=start+Number(b.duration_minutes)+BUFFER_MINUTES,arr=grouped.get(b.date)||[];arr.push({start_minutes:start,end_minutes:end});grouped.set(b.date,arr)}if(date){const blocked=[...(grouped.get(date)||[]),...(await exceptionBlocks(client,date))];return NextResponse.json({blocked,available:slotAvailability(blocked,duration,date)})}const fullyBookedDates=[],first=DateTime.fromISO(`${month}-01`,{zone:business.timezone});for(let d=1;d<=first.daysInMonth;d++){const iso=first.set({day:d}).toISODate(),blocks=[...(grouped.get(iso)||[]),...(await exceptionBlocks(client,iso))];if(!slotAvailability(blocks,duration,iso).length)fullyBookedDates.push(iso)}return NextResponse.json({fullyBookedDates})}catch(e){console.error(e);return NextResponse.json({error:"Availability is temporarily unavailable."},{status:500})}}
export async function POST(req){try{const b=await req.json();if(!b.name?.trim()||!b.email?.trim()||!b.date||!b.start||!b.duration||!b.facial&&!b.body&&!b.eyebrow)return NextResponse.json({error:"Please complete all required fields."},{status:400});const start=mins(b.start),selectedIds=[b.facial,b.body,b.eyebrow?"eyebrow-threading":null].filter(Boolean),services=selectedIds.map(findService).filter(Boolean);if(services.length!==selectedIds.length)return NextResponse.json({error:"One of the selected services is invalid."},{status:400});const duration=services.reduce((sum,s)=>sum+s.duration,0),fa=(b.fa||[]).map(findAddon).filter(Boolean).filter(a=>facialAddonIds().includes(a.id)),ba=(b.ba||[]).map(findAddon).filter(Boolean).filter(a=>bodyAddonIds().includes(a.id)),total=services.reduce((sum,s)=>sum+s.price,0)+fa.reduce((sum,a)=>sum+a.price,0)+ba.reduce((sum,a)=>sum+a.price,0);if(start<OPEN_MINUTES||start+duration+BUFFER_MINUTES>CLOSE_MINUTES||start%SLOT_MINUTES!==0)return NextResponse.json({error:"Please choose one of the available appointment times."},{status:400});const local=DateTime.fromISO(`${b.date}T${b.start}`,{zone:business.timezone});if(!local.isValid||local<=DateTime.now().setZone(business.timezone))return NextResponse.json({error:"Please choose a future appointment time."},{status:400});const end=local.plus({minutes:duration}),blocked=end.plus({minutes:BUFFER_MINUTES}),client=db(),exceptions=await exceptionBlocks(client,b.date);if(exceptions.some(x=>start<x.end_minutes&&start+duration+BUFFER_MINUTES>x.start_minutes))return NextResponse.json({error:"This time is unavailable. Please choose another time."},{status:409});const normalizedEmail=b.email.trim().toLowerCase();let customerId=String(b.customerId||"").trim()||null;
 if(customerId){const {data:p,error}=await client.from("customer_profiles").select("id,name,email").eq("id",customerId).maybeSingle();if(error&&error.code!=="42P01")throw error;if(!p||String(p.email).toLowerCase()!==normalizedEmail)return NextResponse.json({error:"Please choose a valid customer profile for this email."},{status:400})}
 else if(b.newCustomer){const {data:p,error}=await client.from("customer_profiles").insert({name:b.name.trim(),email:normalizedEmail}).select("id").single();if(error&&error.code!=="42P01")throw error;customerId=p?.id||null}
 else {const {data:profiles,error}=await client.from("customer_profiles").select("id,name,email").ilike("email",normalizedEmail).order("name",{ascending:true});if(error&&error.code!=="42P01")throw error;const matches=profiles||[];if(matches.length===1&&matches[0].name.trim().toLowerCase()===b.name.trim().toLowerCase())customerId=matches[0].id;else if(matches.length>0)return NextResponse.json({error:"This email is linked to an existing customer profile. Please choose the correct profile before booking."},{status:409});else {const {data:p,error:pe}=await client.from("customer_profiles").insert({name:b.name.trim(),email:normalizedEmail}).select("id").single();if(pe&&pe.code!=="42P01")throw pe;customerId=p?.id||null}}
 const feeResults=await Promise.all([client.from("fees").select("amount,reason").eq("status","outstanding").ilike("email",normalizedEmail),client.from("customer_fees").select("amount,reason").eq("status","outstanding").eq("customer_id",customerId||"00000000-0000-0000-0000-000000000000"),client.from("customer_fees").select("amount,reason").eq("status","outstanding").ilike("email",normalizedEmail).is("customer_id",null)]);const fees=[...(feeResults[0].data||[]),...(feeResults[1].data||[]),...(feeResults[2].data||[])];if(fees.length){const amount=fees.reduce((x,f)=>x+Number(f.amount),0);return NextResponse.json({error:`There is an outstanding fee of $${amount.toFixed(2)} on this email because of a late cancellation or no-show. Please contact VALE BEAUTY VK before booking another appointment.`},{status:409})}
 const discountQuery=client.from("customer_discounts").select("id,customer_id,email,kind,value,starts_at,expires_at,max_uses,uses,active,note").eq("active",true).lte("starts_at",local.toUTC().toISO());const {data:discounts}=customerId?await discountQuery.or(`customer_id.eq.${customerId},and(customer_id.is.null,email.ilike.${normalizedEmail})`):await discountQuery.ilike("email",normalizedEmail);const validDiscount=(discounts||[]).filter(d=>(!d.expires_at||DateTime.fromISO(d.expires_at)>local.toUTC())&&(!d.max_uses||Number(d.uses)<Number(d.max_uses))).sort((a,b)=>Number(b.value)-Number(a.value))[0]||null;const subtotal=total;let discountAmount=validDiscount?(validDiscount.kind==="percent"?subtotal*Number(validDiscount.value)/100:Number(validDiscount.value)):0;discountAmount=Math.min(subtotal,Math.max(0,discountAmount));const finalTotal=Number((subtotal-discountAmount).toFixed(2));const token=crypto.randomBytes(24).toString("hex"),hash=crypto.createHash("sha256").update(token).digest("hex"),items=[...services,...fa,...ba].map(s=>({name:s.name,price:s.price,duration:s.duration||0}));const row={customer_id:customerId,name:b.name.trim(),email:normalizedEmail,notes:b.notes?.trim()||null,items,service_ids:{facial:b.facial||null,facialAddons:fa.map(x=>x.id),body:b.body||null,bodyAddons:ba.map(x=>x.id),eyebrow:!!b.eyebrow},date:b.date,start_minutes:start,duration_minutes:duration,total_price:finalTotal,subtotal_price:Number(subtotal.toFixed(2)),discount_amount:Number(discountAmount.toFixed(2)),discount_id:validDiscount?.id||null,manage_token_hash:hash,start_at:local.toUTC().toISO(),blocked_end_at:blocked.toUTC().toISO()};const {data:conflicts}=await client.from("bookings").select("id,start_minutes,duration_minutes").eq("date",b.date).eq("status","confirmed");if((conflicts||[]).some(x=>start<Number(x.start_minutes)+Number(x.duration_minutes)+BUFFER_MINUTES&&start+duration+BUFFER_MINUTES>Number(x.start_minutes)))return NextResponse.json({error:"That time was just booked by someone else. Please choose another time."},{status:409});const {error}=await client.from("bookings").insert(row);if(error){if(error.code==="23P01")return NextResponse.json({error:"That time was just booked by someone else. Please choose another time."},{status:409});throw error}
 const manageUrl=`${process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000"}/manage/${token}`,html=`<h2>Your Appointment Is Confirmed ✨</h2><p>Hi ${escapeHtml(b.name.trim())},</p><p><b>${business.name}</b><br>${b.date}<br>${local.toFormat("h:mm a")} – ${end.toFormat("h:mm a")}<br>${duration} minutes</p><p><b>Services</b><br>${items.map(i=>`${escapeHtml(i.name)} — ${i.duration} min — $${Number(i.price).toFixed(2)}`).join("<br>")}<br><b>30 min cleanup buffer</b> — system blocks ${local.toFormat("h:mm a")} → ${blocked.toFormat("h:mm a")}</p><p>Subtotal: $${subtotal.toFixed(2)}<br>${discountAmount>0?`Discount: -$${discountAmount.toFixed(2)}<br>`:""}<b>Total: $${finalTotal.toFixed(2)}</b></p>${b.notes?.trim()?`<p>Notes: ${escapeHtml(b.notes.trim())}</p>`:""}<hr><h3>Cancellation / Appointment Change Policy</h3><p>You must notify us at least 24 hours before your appointment to cancel or change it without a late-notice fee.</p><p><a href="${manageUrl}" style="color:#2879c7">Cancellation / Appointment Change</a></p><p>${business.email}<br>${business.phone}<br>${business.address}</p>`,adminUrl=`${process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000"}/admin`,owner=`<h2>New Appointment — ${business.name}</h2><p><b>Customer:</b> ${escapeHtml(b.name.trim())}<br><b>Email:</b> ${escapeHtml(normalizedEmail)}<br><b>Date:</b> ${b.date}<br><b>Start:</b> ${local.toFormat("h:mm a")}<br><b>Service end:</b> ${end.toFormat("h:mm a")}<br><b>30 min buffer:</b> ${end.toFormat("h:mm a")} → ${blocked.toFormat("h:mm a")}<br><b>System-blocked:</b> ${local.toFormat("h:mm a")} → ${blocked.toFormat("h:mm a")}<br><b>Duration:</b> ${duration} min<br><b>Subtotal:</b> $${subtotal.toFixed(2)}<br><b>Discount:</b> -$${discountAmount.toFixed(2)}<br><b>Total:</b> $${finalTotal.toFixed(2)}</p><p>${items.map(i=>`${escapeHtml(i.name)} — ${i.duration} min — ${Number(i.price).toFixed(2)}`).join("<br>")}</p><p><b>Notes:</b> ${escapeHtml(b.notes?.trim()||"None")}</p><p><a href="${adminUrl}" style="display:inline-block;padding:12px 18px;background:#2879c7;color:#fff;text-decoration:none">🔐 Open Admin Dashboard</a></p>`;const emailResults=await Promise.allSettled([sendMail({to:normalizedEmail,subject:`Appointment Confirmed — ${business.name}`,html}),sendMail({to:business.email,subject:`New Appointment — ${business.name}`,html:owner})]);emailResults.filter(x=>x.status==="rejected").forEach(x=>console.error("BOOKING EMAIL FAILED:",x.reason));if(validDiscount)await client.from("customer_discounts").update({uses:Number(validDiscount.uses||0)+1,active:validDiscount.max_uses&&Number(validDiscount.uses||0)+1>=Number(validDiscount.max_uses)?false:true}).eq("id",validDiscount.id);return NextResponse.json({message:"Please check your email for your appointment confirmation and booking details."})}catch(e){console.error(e);return NextResponse.json({error:"Server error. Please try again or contact the studio."},{status:500})}}
function facialAddonIds(){return ["dermaplaning-addon","led-therapy","high-frequency-full","ice-globe-massage","hot-stone-facial-massage","ultrasonic-deep-infusion","extended-face-neck-massage"]}function bodyAddonIds(){return ["hot-stone-enhancement","scalp-massage","dry-brushing","exfoliation-boost","aromatherapy","moisture-boost"]}function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
