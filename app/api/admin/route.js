import {NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";export async function GET(req){if(req.headers.get("x-admin-pin")!==process.env.ADMIN_PIN)return NextResponse.json({error:"Access denied"},{status:403});const c=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const {data,error}=await c.from("bookings").select("id,date,start_minutes,name,email,notes,duration_minutes,total_price,status").order("date",{ascending:true}).order("start_minutes",{ascending:true});if(error)return NextResponse.json({error:"Database error"},{status:500});return NextResponse.json({bookings:data})}